# Example : 
#       Identifiy the default gateway in the routing table
#
# Expected output : 
#       default            10.112.254.254     UGSc          108        0     en0
#
# Note :
#       network statistics -r to get the routing table
#       default one is the first one.
#       if you use -n the result never ends. 

netstat -nr | grep en0 | grep default
