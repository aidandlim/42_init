# Example 06 : 
#       Get the complete path of the file that contains the IP address of the DNS server you’re using
#
# Expected output : 
#       domain 42.us.org
#       nameserver 10.51.1.253
#
# Note :
#       cat /etc/resolv.conf
#       if you try to cat /etc/resolv.conf, it gives you a notice and it takes you to scutil --dns
#       scutil --dns | grep nameserver
#       scutil is System configuration utility

/etc/resolv.conf
