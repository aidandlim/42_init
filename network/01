# Example : 
#       Get the list of the network interfaces of the machine without displaying any detail for these interfaces. 
#       Only the list of names.
#
# Expected output : 
#       lo0 gif0 stf0 XHC20 en0 en1 en2 en3 p2p0 bridge0 awdl0 utun0

ifconfig -l