# Example 19 : 
#       List all services which are started at boot time and name this kind of services
#
# Note :
#       list-units, list units that systemd currently has in memory.
#       including units that are either referenced directly or through a dependency.
#       Services which start at boot time are called daemon services, also known as background processes.
#       systemctl list-unit-files --type=service | grep "enabled"

systemctl list-units --type=service
