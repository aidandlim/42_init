# What file contains the RSA keys of systems that are authorized to connect via SSH?
#
# Expected output : 

This file is called authorized_keys.
It is located on .ssh folder(which is different for each users).
~/.ssh/authorized_keys