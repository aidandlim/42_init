# Example 02 : 
#       What command can you use to rename your system?
#       And if you want to check it works, type hostname
#       if you get 'command not found', add path '/bin' to .bashrc file and then restart terminal

hostname [NEW_HOST_NAME]
