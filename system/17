# Example : 
#       Find the command that allows you to connect via ssh on the VM.(In parallel with the graphic session)
#
# Note :
#       first make sure you install/configure SSH after that
#       you can connect to via ssh on the VM
#       find the IP address with if config inet addr being the local IP

ssh user@ip